export const typeDef = `
  type Allocation {
    name: String
    id: String
    startFrequency: Int
    stopFrequency: Int
    powerUsage: Int
    customerId: String
    originatorId: String
  }
`;

